name: Project Updates to Discord
on:
  issues:
    types: [opened, closed, edited]
  pull_request:
    types: [opened, closed, merged]

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
    - name: Get Project Status
      uses: actions/github-script@v6
      with:
        script: |
          // Projects API를 통해 카드 상태 확인
          // Discord 웹훅으로 알림 전송
