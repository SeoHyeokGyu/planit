name: Discord Issue All Event Notifier

on:
  issues:
    types: [opened, edited, deleted, transferred, closed, reopened, assigned, unassigned, labeled, unlabeled, milestoned, demilestoned, locked, unlocked]

jobs:
  notify_discord:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          ACTION: ${{ github.event.action }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
          SENDER_LOGIN: ${{ github.event.sender.login }}
          UPDATED_AT: ${{ github.event.issue.updated_at }}
        run: |
          # Step 1: Set title and color based on the event action
          case $ACTION in
            "opened") TITLE="🚨 새로운 이슈 발생: #$ISSUE_NUMBER"; COLOR=16711680 ;;
            "closed") TITLE="✅ 이슈 해결: #$ISSUE_NUMBER"; COLOR=65280 ;;
            "reopened") TITLE="⚠️ 이슈 재개: #$ISSUE_NUMBER"; COLOR=16761088 ;;
            "labeled") TITLE="🏷️ 라벨 추가: #$ISSUE_NUMBER"; COLOR=4390561 ;;
            "unlabeled") TITLE="🏷️ 라벨 제거: #$ISSUE_NUMBER"; COLOR=4390561 ;;
            "assigned") TITLE="👤 담당자 지정: #$ISSUE_NUMBER"; COLOR=4390561 ;;
            "unassigned") TITLE="👤 담당자 해제: #$ISSUE_NUMBER"; COLOR=4390561 ;;
            "edited") TITLE="✏️ 이슈 내용 수정: #$ISSUE_NUMBER"; COLOR=16776960 ;;
            "locked") TITLE="🔒 이슈 잠금: #$ISSUE_NUMBER"; COLOR=4390561 ;;
            "unlocked") TITLE="🔓 이슈 잠금 해제: #$ISSUE_NUMBER"; COLOR=4390561 ;;
            *) TITLE="⚙️ 이슈 업데이트: #$ISSUE_NUMBER"; COLOR=4390561 ;;
          esac

          # Step 2: Prepare the description text
          DESCRIPTION=$(printf '**액션:** %s\n**제목:** %s\n**처리자:** %s\n\n[이슈 바로가기](%s)' "$ACTION" "$ISSUE_TITLE" "$SENDER_LOGIN" "$ISSUE_URL")

          # Step 3: Generate JSON with jq and pipe it directly to curl for sending
          jq -n \
            --arg title "$TITLE" \
            --arg description "$DESCRIPTION" \
            --argjson color "$COLOR" \
            --arg timestamp "$UPDATED_AT" \
            '{embeds: [{title: $title, description: $description, color: $color, timestamp: $timestamp}]}' | \
            curl -X POST -H "Content-Type: application/json" -d @- "$DISCORD_WEBHOOK_URL"
