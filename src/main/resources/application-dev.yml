spring:
  # Neon PostgreSQL (로컬 테스트)
  datasource:
    url: jdbc:postgresql://ep-gentle-rain-a17vm76u-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_E8grzvON4UwY
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Redis 비활성화 (Upstash 설정 전까지)
  data:
    redis:
      repositories:
        enabled: false

logging:
  level:
    com.planit: DEBUG
    org.springframework: INFO
